{
  "name": "goarch-helper-action",
  "private": true,
  "version": "1.0.0",
  "description": "Github Action to format OS/ARCH values to useful variants",
  "main": "index.js",
  "scripts": {
    "build": "ncc build arch-normalize.js -m -o .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@vercel/ncc": "^0.25.1"
  }
}
